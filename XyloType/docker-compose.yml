services:
  seq:
    image: datalust/seq:latest
    container_name: seq
    environment:
      ACCEPT_EULA: Y
      SEQ_FIRSTRUN_ADMINPASSWORD: ${SEQ_PASSWORD}
    ports:
      - "5341:80"
    volumes:
      - seq-data:/data

volumes:
  seq-data: